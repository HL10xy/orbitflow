from agents.base import BaseAgent


class ArchitectAgent(BaseAgent):
    role = "architect"
    icon = "\U0001f3d7️"
    context_hint = "Focus on: architecture decisions, component boundaries, data flow, and technology choices. Output structured markdown."
